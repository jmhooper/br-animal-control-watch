require("../stylesheets/header.scss")

import React from "react"

const Header = (props) => (
  <header className="header">
    <h1 className="header__brand">BR Animal Control Incidents</h1>
  </header>
)

export default Header
